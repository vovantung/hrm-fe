'use client'

import SagaPage from '@/views/admin/saga/SagaPage'

// import { useParams } from 'next/navigation'

const AdminPage = () => {
  // const { id } = useParams()

  return <SagaPage />
}

export default AdminPage
